# /etc/strongswan.conf
charon {
    load_modular = yes
    plugins {
        include strongswan.d/charon/*.conf

        vici {

        }

        eap-radius {
            # Both services in host network mode, RADIUS accessible on localhost
            server = 127.0.0.1:1812
            # Must match RADIUS_SECRET env in the radius service
            secret = HpE98gAFA4OaJaHYU46M
            accounting = yes
        }
    }
}
